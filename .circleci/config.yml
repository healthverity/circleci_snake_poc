version: 2.1
workflows:
  testing:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: runnerpoc/test
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: echo $(pwd)
      - run: echo $(ls -lat)
